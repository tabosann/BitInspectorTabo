<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BitInspectorTabo.Pages.InspectorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BitInspectorTabo.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:BitInspectorTabo.Controls"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/DescriptionStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <StackPanel Padding="36,24,36,0" Spacing="32">
            <TextBox x:Uid="TextBoxHexUid" Text="{x:Bind IPVM.Hex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <interactivity:Interaction.Behaviors>
                    <local:BeforeTextChangingEventBehavior Command="{x:Bind IPVM.TextBoxHexBeforeTextChangingCommand}"/>
                </interactivity:Interaction.Behaviors>
            </TextBox>

            <TextBox x:Uid="TextBoxBinUid" Text="{x:Bind IPVM.Bin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <interactivity:Interaction.Behaviors>
                    <local:BeforeTextChangingEventBehavior Command="{x:Bind IPVM.TextBoxBinBeforeTextChangingCommand}"/>
                </interactivity:Interaction.Behaviors>
            </TextBox>

            <StackPanel Spacing="8">
                <TextBox x:Uid="TextBoxFloatUid" Text="{x:Bind IPVM.Float, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <interactivity:Interaction.Behaviors>
                        <local:BeforeTextChangingEventBehavior Command="{x:Bind IPVM.TextBoxFloatBeforeTextChangingCommand}"/>
                    </interactivity:Interaction.Behaviors>
                </TextBox>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <TextBlock x:Uid="HexLabelUid" Width="50" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                    <TextBlock Text="{x:Bind IPVM.FloatHex, Mode=OneWay}" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <TextBlock x:Uid="BinLabelUid" Width="50" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                    <TextBlock Text="{x:Bind IPVM.FloatBin, Mode=OneWay}" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBox x:Uid="TextBoxDoubleUid" Text="{x:Bind IPVM.Double, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <interactivity:Interaction.Behaviors>
                        <local:BeforeTextChangingEventBehavior Command="{x:Bind IPVM.TextBoxDoubleBeforeTextChangingCommand}"/>
                    </interactivity:Interaction.Behaviors>
                </TextBox>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <TextBlock x:Uid="HexLabelUid" Width="50" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                    <TextBlock Text="{x:Bind IPVM.DoubleHex, Mode=OneWay}" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <TextBlock x:Uid="BinLabelUid" Width="50" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                    <TextBlock Text="{x:Bind IPVM.DoubleBin, Mode=OneWay}" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextBoxDisabledForegroundThemeBrush}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
